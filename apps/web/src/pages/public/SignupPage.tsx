import { AuthCard } from '@/features/auth/AuthCard'

export function SignupPage() {
	return <AuthCard compact title="Create your account" description="The signup surface stays light and focused on entry into the product." submitLabel="Create account" mode="signup" />
}
