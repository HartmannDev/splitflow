Before making any UI change, always read `.interface-design/system.md` if it exists.
Treat existing component patterns as the default source of truth.
Do not introduce new visual patterns unless they are reusable and justified.